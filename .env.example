# Dasch Dashboard Environment Variables

# Node Environment
# Set to "production" for production deployments
# In development, secrets can be stored unencrypted for easier testing
NODE_ENV=development

# Secrets Master Key
# Required in production mode
# Generate a key by running: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: Keep this key secure and back it up! Losing it means losing all encrypted secrets.
SECRETS_MASTER_KEY=

# Server Port (optional, defaults to 3001)
PORT=3001
